# Pinned to tested versions (Apr 2026). Upgrade cautiously — pandas-ta
# breaks on NumPy 2.x, so keep numpy < 2 until pandas-ta ships a fix.

# Core
requests==2.33.1
python-dotenv==1.0.1

# Bot 1 momentum strategy (indicators + Excel journal)
numpy==1.26.4
pandas==2.2.3
pandas-ta==0.3.14b0
openpyxl==3.1.5

# Timezones (used by dashboard's America/Chicago; stdlib zoneinfo on Python 3.9+
# is sufficient but pytz is kept as a fallback on slim containers)
pytz==2024.2

# Test-only (not installed by setup.sh unless dev mode)
# pytest==9.0.3
